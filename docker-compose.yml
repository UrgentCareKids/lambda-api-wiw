version: "3.8"
services:
  lambda-api-wiw:
    container_name: ${CONTAINER_NAME}
    build:
      dockerfile: Dockerfile
      context: ./
    ports:
      - 9090:8080
    env_file:
      - .env/local
    volumes:
      - ./src:/var/task
      - ./db:/var/task/db
      - ./tests:/var/tests